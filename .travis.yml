language: python
python:
  - "2.7"
  - "3.3"

install:
  - "npm install less clean-css coffee-script uglify-js"
  - "pip install -r requirements.txt"

script: "export PATH=$PATH:$(pwd)/node_modules/.bin python manage.py test"
